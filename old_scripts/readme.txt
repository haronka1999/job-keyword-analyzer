this list will contains the information how I migrated the data from here to the main:

linkedsent.py:
    1. get_job_description_url_list() --> retrieve links
    2. scrape_job_description() --> from the links we will create the job_descirption text

preprocess.py:
    1. please choose any function what you need from there

get_keywords.py:
    1. Choose any language model which will extract the